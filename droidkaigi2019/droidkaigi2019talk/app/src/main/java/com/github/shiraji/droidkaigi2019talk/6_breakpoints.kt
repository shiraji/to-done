package com.github.shiraji.droidkaigi2019talk

// Confirm delete Breakpoints!
// Preferences | Build, Execution, Deployment | Debugger

fun fooBreakpoints(bar: Int) {
    if (bar != 5) {
        println(bar)
    }
}